# memory — project-scoped durable notes

English | [中文](README.zh.md)

Plain-markdown memory per workspace, tool-shaped: content reaches a model only through explicit `memory` calls, never injection.

| Package | Role |
|---|---|
| [`memory/`](memory/README.md) | Memory service seam + local filesystem provider + model-facing tool |
